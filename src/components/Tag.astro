---
interface Props {
  label: string;
  /** Classe de couleur optionnelle : lang, front, back, cloud, test, method. */
  variant?: string;
}

const { label, variant } = Astro.props;
---

<span class={["tag", variant].filter(Boolean).join(" ")}>{label}</span>
