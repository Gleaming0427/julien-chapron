---
import Layout from "../layouts/Layout.astro";
import Section from "../components/Section.astro";
import AppCard from "../components/AppCard.astro";
import { apps } from "../data/cv";
---

<Layout
  title="Apps & outils — Julien Chapron"
  description="Les applications et outils de Julien Chapron : L'Atelier CoQuest, Pims Pocket, Studio CoQuest, api-gateway-core, cve-agent."
>
  <header class="page-head reveal">
    <h1><span class="prompt">$</span> ls ~/apps</h1>
    <p class="sub">Galerie des applications et outils que j'ai conçus et développés.</p>
  </header>

  <Section command="find . -name '*.app'">
    <div class="apps-list">
      {apps.map((app) => <AppCard app={app} />)}
    </div>
  </Section>
</Layout>
